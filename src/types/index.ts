export type TProps = {
  navigation: any;
};

export type TProduct = {
  id: number;
  key: string;
  name: string;
  imageUrl: string;
};

export type TDetail = {
  imageUrl: string;
  name: string;
  description: string;
  berat:number
}
